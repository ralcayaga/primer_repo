/**
 * * 
 * ? 
 * !
 * TODOS
 * @param num1 
 * @param num2 
 * @returns  number
 * TODOS 
 */

function sumarNumeros (num1: number, num2:number){
    //? Eso es una consulta 
    return num1 + num2;
}

function restaNumeros (num1: number, num2:number){
    return num1 - num2;
}